using ipw240x;
using nsoftware.IPWorks;
using nsoftwareKeygen;

h.init();
var ipw_serial = KeyGenerator.GenerateRuntimeKey(nsoftwareProductType.IPWorks);
M.n(98, typeof(CalDAV), ipw_serial.RuntimeKey);

var dns = new DNS(ipw_serial.RuntimeKey);